apiVersion: v1
kind: Service
metadata:
  name: nodeport-echo
  namespace: ebpf-nodeport-test
spec:
  type: NodePort
  externalTrafficPolicy: Cluster
  selector:
    app: nodeport-echo
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080
      protocol: TCP
